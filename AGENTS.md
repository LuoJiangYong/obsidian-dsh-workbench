# DeepSeek Harness Workbench 开发宪法

本文件是本仓库所有开发会话的最高项目级规则。除运行时原文、产品标识、代码标识、协议标识、命令、路径和社区要求的英文声明外，项目治理文档、开发说明、验收报告和用户界面默认使用中文。

固定名称不得自行修改：

- 社区显示名：`DeepSeek Harness Workbench`
- 插件 ID：`deepseek-harness-workbench`
- GitHub 仓库：`obsidian-dsh-workbench`
- 身份声明：`Unofficial community integration for DeepSeek Harness.`
- 首版平台声明：`isDesktopOnly: true`

## 1. 权威来源与能力真相

规则冲突时依次服从：

1. 用户对当前批次的明确批准与范围变更。
2. 本文件。
3. 已批准的 ADR、公共协议、阶段计划和发布门。
4. 实时代码、测试、验证脚本、构建产物和读回结果。
5. DeepSeek Harness、Obsidian 等官方文档和官方仓库。
6. 旧规划、交接摘要、历史提示词、实验草稿和生成内容。

每次开始工作必须重新读取实时仓库、Git 状态和相关权威文件。不得依靠聊天记录推断已实现状态。

所有结论必须区分：`已实现并验证`、`已实现但未完成运行验收`、`仅规划`、`未验证`、`阻塞`、`明确延期`。接口、类型、空视图、proposal、测试桩和私有协议不等于用户已经可用的完整功能。GitHub 仓库、`manifest.json` 或 Release 不等于 Obsidian 社区名称已经占位。

### 1.1 UI 审阅真相分层

- [Ardot 项目 `DeepSeek Harness Workbench · UI 真相`](https://ardot.tencent.com/file/718186366720195)的最新获用户批准版本，是用户审阅的产品 UI 界面真相。
- `DESIGN.md` 是 Ardot 设计的稳定文字契约与实现镜像；实时源码、测试、CI 和运行读回仍是功能是否已经交付的能力真相。
- 未批准草稿、实验画板、导出截图和第三方参考图不能授权实现，也不能改变当前批次范围。
- 布局、页面、导航、图标、状态文案或响应式规则变化，必须同步演进 Ardot、`DESIGN.md`、相关 ADR、实现、测试、CI 契约和 `design-qa.md`；若批准批次只更新其中一层，必须登记差异和后续同步门。
- 每个 UI 批次必须记录 Ardot 文件、用户审阅版本和页面/画板 ID，并确认 CI 实际执行治理契约测试；Ardot 设计通过不得冒充 Obsidian 运行验收通过。
- Ardot 产品画板和 Obsidian 插件界面只呈现用户完成任务所需的产品语义，不显示“首发”“规划中”“尚未实现”等开发阶段、发布批次或治理审批文案。尚未开放但需要保留的导航项必须使用浅灰文字与图标、原生 `disabled` 或 `aria-disabled="true"` 表达不可用；发布门和实现差异只记录在治理文档、测试、CI 与验收证据中。

## 2. 审批门控与最小批次

一次只实施一个明确批准的最小批次。每批开始前必须写明：

- 用户可见或契约可验证的唯一结果。
- 允许范围与禁止范围。
- 预计修改文件。
- 验收证据与验证命令。
- 失败回退。
- 停止边界。

发现邻近问题只登记，不得借“顺手修复”扩大范围。完成当前批次后必须停止；下一批需要新的用户确认。当前提示词明确包含多个连续交付物时，可按其中已经列明的顺序完成，但不得越过提示词的最终停止边界。

## 3. 第一性原理与实现纪律

重要功能、抽象、重构或优化前必须回答：

1. 改善哪个具体用户工作流？
2. 哪些身份、隐私、安全、产品和协议边界不可改变？
3. 最小可行机制是什么？
4. 当前真实瓶颈有什么测试、错误或操作证据？
5. 保持现状、小改和结构调整分别有什么代价与回滚路径？
6. 用什么测试、构建、读回或运行证据证明完成？

没有至少两个真实消费者和可复现的重复痛点，不建立公共抽象。公共契约变化必须同步检查注册、配置、持久化、下游、文档、测试、模板和 CI；无需同步时也要明确记录检查结论。

## 4. AI 代码开发原则

### 4.1 先想再写（Think Before Coding）

修改第一行代码前必须：

- 用一句话说明本批次要改善的用户结果。
- 明确允许范围、禁止范围和停止边界。
- 读取实时源码、测试、公共契约和配置，不根据文件名、历史摘要或记忆猜测。
- 区分已验证事实、合理推断和未验证假设。
- 定义输入、处理、状态、输出以及失败行为。
- 写明最小成功标准和验证命令。
- 存在多种合理解释时说明差异；会实质改变产品、安全或数据边界时先请求确认。

禁止把不确定性偷偷固化进代码，禁止用占位实现、假数据或宽松 fallback 掩盖未知事实，禁止在没有失败基线或行为证据时进行大规模重构。

通过标准：编码前已经能回答“为什么改、改什么、不改什么、如何证明完成”。

### 4.2 极简优先（Simplicity First）

- 只实现满足当前验收条件的最小机制。
- 不为未批准的未来需求增加配置、抽象、扩展点或兼容分支。
- 优先复用现有稳定契约，不创建第二套事实来源。
- 至少有两个真实消费者且存在可复现的重复痛点，才考虑公共抽象。
- 局部重复如果比错误抽象更清楚，可以暂时保留。
- 不添加“以防万一”的静默重试、错误吞噬和自动降级。
- 性能优化必须有测量证据。
- 不使用虚假成功状态；失败必须可见、可定位、可验证。

完成前必须检查：是否存在删除后仍能满足需求的代码，是否增加了当前没有消费者的灵活性，是否能以更少依赖和更小失败范围实现相同结果，是否用复杂抽象替代了清楚的局部实现。

### 4.3 精准修改（Surgical Changes）

- 每处变更都必须能映射到当前批次的验收条件。
- 不顺便重构、改名、统一格式或修复无关问题。
- 保持现有命名、结构、语言和代码风格。
- 保留用户已有修改，不覆盖不属于本批次的内容。
- 发现额外问题时登记为候选，不自动扩大范围。
- 清理本批产生的未使用 import、变量、临时代码、调试输出和失败生成物。
- 修改公共契约时，同步检查类型和枚举、注册与路由、配置与持久化、下游消费者、文档与示例、测试和 CI。
- 检查后无需同步，也要明确记录“已检查，无需修改”。

通过标准：审阅 diff 时，每一处修改都能说明它具体服务于哪个验收条件。

### 4.4 目标驱动（Goal-Driven Execution）

开发按以下闭环执行：

```text
定义结果与验收条件
→ 读取实时实现
→ 建立失败基线或契约基线
→ 实施最小修改
→ 运行最相关测试
→ 补齐或更新 CI 覆盖
→ 运行完整质量门
→ 检查依赖和边界
→ 清理生成物
→ 复核 diff
→ 精确提交和 push
→ 确认远端 CI 与 annotations
→ 到达批准边界后停止
```

- Bug 修复应先复现，再修改。
- 新功能应先定义输入、输出、状态和错误行为。
- 重构必须证明行为等价，或明确列出行为变化。
- 先运行最小相关测试，再运行完整质量门。
- 写入文件或持久状态后必须读回。
- 验证失败时先定位原因，不通过扩大修改范围碰运气。
- 不以“代码已经写完”作为完成标准。
- 不因测试通过自动声称 UI、运行时、平台或发布验收通过。
- 下一批未获批准时必须停止。

### 4.5 AI 禁止模式

禁止：

- 未读取实时实现就生成替代实现。
- 编造不存在的 API、事件、测试结果或平台能力。
- 使用 `any`、无界类型断言或空 catch 掩盖契约问题。
- 为让测试通过而降低断言强度、删除失败用例或跳过测试。
- 用 mock 通过冒充真实 DSH、Windows 或 Obsidian 验收通过。
- 创建没有当前消费者的通用框架。
- 自动安装依赖、修改全局环境或真实 Vault。
- 未经批准提交、发布或扩大外部副作用。
- 把 CI 成功解释为下一阶段已经批准。
- 留下未披露的 TODO、临时 fallback 或已知失败路径。

### 4.6 AI 与 CI 的强制联动

每次 AI 修改代码时必须回答：

1. 哪个行为发生了变化？
2. 哪个测试证明该行为？
3. CI 是否实际执行该测试？
4. 是否需要更新 Windows、协议、安全、Vault 或 Release job？
5. 若未覆盖，为什么仍可完成本批次？

如果第三项答案为否，必须在同一批次更新 CI；否则当前批次不得标记完成。

## 5. 用户文件与 Git 安全

- 默认保留所有用户已有文件和未提交修改。
- 禁止未经授权执行 `reset`、`checkout`、覆盖、清理或批量格式化。
- 只暂存当前批准批次的精确文件；禁止 `git add .`、`git add -A` 和 `git add --all`。
- 构建产物、临时文件、测试运行数据和失败生成物必须清理或明确忽略。
- Commit 使用 `<type>: <中文简述>`；每个 commit 对应一个可独立验证和回滚的结果。
- push 后必须确认远端 CI；失败时停止进入下一批并如实报告。

## 6. Vault 与数据安全

Vault 默认只读。任何 Vault 写入必须遵循：

```text
proposal → 路径和版本校验 → diff → 用户确认 → Obsidian API 写入 → 读回验证
```

- 未经批准禁止写入、删除、移动或批量修改 Vault 内容。
- 不把整个 Vault 默认设为 DSH 可写 `cwd`。
- DSH 会话、缓存、索引、日志和凭据放在 Vault 外。
- 禁止默认开放任意 Shell、全磁盘访问和危险模式。
- 删除、批量移动、Vault 配置修改和写入范围扩大必须单独确认。
- 插件开发和破坏性测试只能使用隔离测试 Vault；不得使用真实个人知识库。
- 外部服务继续作为其数据的事实权威；Vault 只保存用户明确选择的快照或产物。

## 7. 外部运行时与 Windows 进程

- 禁止自动安装或更新 DSH、Node、Python、MCP Server 或其他外部依赖。
- DSH 版本必须锁定，并维护运行时与协议兼容矩阵。
- 不能把关闭整个运行时冒充为 mid-turn cancel。
- Windows 必须验证裸命令、绝对路径、`.cmd` shim、启动、握手、取消、正常关闭、强制终止回退和无残留进程。
- 子进程必须使用隐藏窗口，stderr 只保留脱敏且限长的诊断尾部。
- 插件禁用或卸载时必须终止所有受管进程并移除监听器。
- SDK、ACP 和自定义 bridge 的能力必须来自当前官方契约与实测，不能从规划推断。

## 8. 隐私、网络与依赖

- 禁止客户端遥测。
- 网络端点、账号要求、外部文件访问和数据流向必须在 README 与设置界面披露。
- 不保存明文密钥，不把凭据、笔记全文或本机绝对路径写入日志和发布物。
- 依赖必须锁定并提交 lockfile。
- 新增依赖必须说明用途、替代方案、体积、安全边界和许可证。
- 插件不得自行安装、更新或下载可执行依赖；只能检查用户已经配置的外部运行时并给出指引。

## 9. 平台与发布声明

- 首版固定 `isDesktopOnly: true`，不得暗示移动端兼容。
- README 和社区描述必须持续声明非官方集成。
- 不宣称“首个”或“官方”DeepSeek Harness 插件。
- Release tag、`manifest.json.version`、`package.json.version`、`versions.json` 和附件必须一致。
- 未完成隔离 Vault 验收、进程清理验收或仍有自动审核阻塞时，不得发布或提交社区目录。
- 首个 Obsidian 社区插件发布功能固定为“新建任务”。在其对话、任务执行、上下文、权限和错误状态完整实现，相关 CI 与隔离 Vault 运行验收通过，并获得用户对最终运行 UI 的明确验收前，不得进入社区发布审批。
- 创建 GitHub 仓库、写入 manifest 或创建 Release 不得表述为社区 ID 已占位；只有社区目录接受并发布后才是 `已占位`。

## 10. 完整 CI 覆盖与伴随演进

CI 是开发宪法的自动执行器，必须伴随实现共同演进。基础 CI 至少覆盖：

- 锁文件安装：`npm ci`
- TypeScript：`typecheck`
- 代码规范：`lint`
- 单元、契约与假运行时测试：`test`
- 生产构建：`build`
- manifest、package、versions 和 tag 一致性
- 仓库边界与禁止文件检查
- 凭据和敏感文件扫描
- 运行时协议、超时、取消和进程清理测试
- Windows 专项进程测试
- Release 资产结构与版本校验
- 隔离 Vault 验收入口

每个开发批次必须：

1. 判断新增或修改的行为是否已有 CI 覆盖。
2. 没有覆盖时，在同一批次补测试和 CI。
3. 测试存在但 workflow 未执行时，同步更新 workflow。
4. 修改平台、协议、发布或安全边界时，复查完整 CI 矩阵。
5. 本地验证与 CI 尽量共用脚本，避免两套规则漂移。
6. push 后确认 CI 成功；平台提供 annotations 时要求原始 `0 annotations`。
7. CI 通过只代表自动门禁满足，不自动批准下一批或发布。

CI 分层及实施状态以 `docs/ci-cd-roadmap.md` 为权威。Phase E Release Automation 必须单独批准，不得自动提交社区目录。

## 11. Definition of Done

每个批准批次完成前必须具备：

- 精确范围内的实现及对应测试。
- CI 覆盖检查和必要的 workflow 更新。
- `lint`、`typecheck`、`test`、`build` 和边界检查通过。
- 公共契约及下游影响已检查。
- 临时文件和生成物已清理。
- diff 已复核，治理文档与实时能力一致。
- task-scoped commit 和 push。
- 远端 CI 成功且无未处理 annotations。
- 已到达停止边界并等待下一次批准。

如果当前仓库、远端或 CI 尚不存在，应明确标记相应条目为 `未建立` 或 `延期，未通过`，不得省略后声称完成。

## 12. 当前禁止自动进入的范围

- 完整聊天工作台。
- “新建任务”的 DSH 对话、任务执行和上下文运行实现；当前只批准 Ardot v2 与治理契约，实际实现需要新的明确批次。
- Vault 写入和删除。
- 任意 Shell。
- DSH 自动安装或更新。
- Dida MCP、微信读书 Skill、微信文章抓取。
- 资讯雷达、行业雷达、房地产工作台、人生仪表盘。
- 移动端实现。
- 遥测。
- fork Claudian 或 DeepSeek Harness 作为长期产品基线。

上述能力只有在成为新的明确批准批次后才能进入开发。
