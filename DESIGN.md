# DeepSeek Harness Workbench 设计规范

状态：已批准的产品 UI 目标；运行实现部分未同步。最新获用户批准的 [Ardot 设计项目](https://ardot.tencent.com/file/718186366720195)是用户审阅的产品 UI 界面真相；本文件是其稳定文字契约与实现镜像。

Ardot 负责页面、布局、状态、图标、文案层级和响应式外观的审阅真相；本文件负责把已批准设计转写成可测试规则。源码、测试、CI 和运行读回仍是“是否已经实现并可用”的能力真相，Ardot 画板不得被解释为功能已经交付。逐批视觉验收证据记录在 `design-qa.md`。

## 0. Ardot 审阅基线

- 项目：`DeepSeek Harness Workbench · UI 真相`
- 文件 ID：`718186366720195`
- 用户审阅版本：`v2`，批准日期 `2026-08-24`
- 页面：`UI 真相 v2`（`12:1`）；`UI 真相 v1`（`0:1`）只作为历史与失败回退基线保留。
- 产品画板：`00 v2 设计系统与交互状态`（`12:2`）、`01 新建任务 宽屏浅色`（`12:41`）、`02 运行 宽屏浅色`（`12:120`）、`03 新建任务与快速助手 宽屏浅色`（`12:191`）、`04 新建任务 宽屏深色`（`12:275`）、`05 新建任务 700px 容器`（`12:360`）。
- 同屏 QA 画板：`06 参考截图与 v2 对照 QA`（`12:530`）。

只有最新获用户批准的 Ardot 版本可以授权实现同步；未批准草稿、实验副本和导出截图都不能单独改变产品能力或开发范围。

## 1. 产品定位

DeepSeek Harness Workbench 是 Obsidian 内的中央智能体工作台，不是常驻聊天侧边栏，也不复制 Obsidian 自己的标题栏、ribbon、标签栏、文件导航或全局设置。

目标主工作流是：用户从 Obsidian 最左侧 ribbon 或命令面板打开一个可复用的中央 Workbench 标签页，默认进入导航首位的“新建任务”，与 DeepSeek Harness 对话、定义任务、选择上下文并在执行前审阅权限与变更；需要时可以单独打开右侧快速助手。导航最后的“运行”汇总概览真值与只读运行状态。

当前实时运行代码仍只开放分离的“概览”和“运行状态”，没有“新建任务”对话、任务执行或上下文能力。本 v2 是`仅设计已更新，运行代码未同步`，不得把目标工作流描述为已经交付。

## 2. 参考层级

视觉与布局判断依次参考：

1. 最新获用户批准的 Ardot 审阅版本。
2. 本文件和已接受的架构 ADR。
3. 当前仓库的真实源码、测试、构建与 Obsidian 运行结果，用于判断实现差异和能力真相。
4. Trend Radar 仓库 `DESIGN.md` 中已经验证的 Obsidian 原生工作台规则。
5. 用户提供的 WorkBuddy 截图，仅参考“首位新建任务 + 产品自有左导航 + 中央任务输入”的信息架构，不复制品牌、账号、历史任务、模型名称、第三方服务入口或其他未实现数据。

参考图与本插件能力冲突时，能力真相优先；不得为了视觉完整度制造可点击但不可用的功能或虚构数据。

## 3. 三层工作台结构

### 3.1 Obsidian 宿主入口

- 一个稳定 ribbon 图标打开或复用中央 Workbench 标签页。
- 命令面板提供相同入口。
- Workbench 不默认占用 Obsidian 右侧边栏。
- 插件不重建 Obsidian 全局 chrome。

### 3.2 Workbench 内部导航

- 中央标签页内由插件拥有一条左侧本地导航栏。
- 桌面宽屏导航宽度固定为 `194px`。
- v2 目标导航首位固定为“新建任务”，承担主对话、任务执行、上下文、权限和变更审阅；该能力当前尚未实现。
- “项目”“专家 · Skill · 连接器”“自动化”“资料库”“领域工作台”位于中间；暂不可用时只使用浅灰文字与图标的禁用态，不显示“规划中”、开发进度或发布批次徽标。
- “概览”和“运行状态”合并为“运行”，固定在功能导航最后；当前审阅名为“运行”，后续改名必须同步 Ardot、本文件、ADR、实现、测试和 CI 契约。
- 右侧“快速助手”不占用内部功能导航项；它仍是单独命令按需打开的可选视图。
- 暂不可用入口不得注册点击行为、加载假数据或暗示对应能力已经可用。
- 小于或等于 `760px` 的容器宽度隐藏固定左栏，改用主内容顶部的紧凑页面选择器。
- 当前源码仍渲染分离的“概览”“运行状态”和禁用“助手”入口；这是明确登记的实现差异，不属于本设计批次的完成项。

### 3.3 可选右侧快速助手

- 快速助手是独立、按需打开的 Obsidian 右侧视图，不随 Workbench 自动打开。
- v2 把快速助手定义为可选能力，只辅助展示 DSH 健康、当前上下文和快捷提问，不承担主对话；产品界面标题只显示“快速助手”。
- 当前实现只展示 DSH 健康状态、当前上下文空状态和快捷提问不可用说明。
- 当前不得显示可编辑对话框、发送、停止、模型选择、会话历史或伪造回复。
- 后续会话能力、上下文读取和快捷提问必须分别获得新批次批准并建立安全与 CI 证据。

## 4. 页面职责

### 4.1 新建任务

“新建任务”是 v2 的核心工作流，也是未来唯一主对话与任务入口。

- 主标题、模式、输入区和执行确认只显示产品语义，不显示“首发目标”“尚未实现”、开发阶段、发布批次或治理审批文案。
- 首屏提供“对话”“任务执行”“代码协作”模式层级；当前 Ardot 只定义视觉与信息架构，不授权注册运行行为。
- 主输入区包含目标描述、`@` 上下文引用、`/` Skill 或命令入口、附件、上下文、默认权限、DSH 模型管理提示和发送操作。
- 输入、上下文、权限、模型和发送都不得使用假数据；未接入 DSH 时必须不渲染该可用页面，或把对应操作设为真实禁用态，不得用开发状态文案代替行为约束。
- 执行前必须向用户展示当前上下文、权限和拟变更内容，并提供取消路径；Vault 写入仍遵循独立审批协议。

### 4.2 运行

“运行”合并原“概览”和“运行状态”，回答两个问题：当前工作台真实可用到什么程度，以及当前配置的 DSH 命令能否通过受限的 `--version` 检查？

- 首屏概览摘要展示工作台壳层、DSH 健康检查、会话能力和 Vault 权限。
- 下方保留连接状态、运行时健康检查、Vault 权限和平台四项真实状态。
- 唯一主操作是“检查 DSH”。检查中禁用重复操作并显示进行中状态。
- 健康检查成功只表示命令可执行且版本匹配，不表示 DSH 已连接。

### 4.3 首个社区发布门

首个 Obsidian 社区插件发布功能固定为“新建任务”。只有同时满足以下条件，才允许进入社区发布审批：

1. “新建任务”的对话、任务执行、上下文、权限与错误状态完整实现。
2. 测试和双平台 CI 实际覆盖相应行为，隔离 Vault 运行验收通过。
3. 用户对最终 Obsidian 运行界面完成 UI 验收并明确批准。

Ardot 设计通过、CI 成功或 GitHub Release 单独成立时，都不能越过此发布门。

## 5. 视觉语言

- 使用 Obsidian 语义变量：`--background-primary`、`--background-secondary`、`--background-modifier-border`、`--background-modifier-hover`、`--interactive-accent`、`--text-normal`、`--text-muted` 和 `--text-faint`。
- 插件身份图标使用 DeepSeek Harness Web 前端 `@deepseek-ai/dsh-web-frontend@0.1.1-rc.1/dist/favicon.svg` 的鲸鱼矢量，源文件 SHA-256 为 `C61A62A9D47D8660F9CFE08AAC6775FF0476F7D6C5053F7659C1F8493FD6D814`。Ardot 中用于 Obsidian ribbon、活动标签页、Workbench 左上角和快速助手；深色模式以白色承载底保证黑色原始矢量的对比度，不重绘品牌图形。
- Workbench 左上角品牌名称固定为三行：第一行 `DeepSeek`、第二行 `Harness`、第三行 `Workbench`；不得合并成两行或增加副标题。
- 其他功能图标使用 Obsidian 内置或官方 Lucide 图标；图标辅助识别但不替代文字标签。“新建任务”使用 Lucide `circle-plus.svg`，来源 SHA-256 为 `6FA978530075DF975AD05E2742896A1950B74C2B133A0D208891B038A4A41C17`。
- 鲸鱼图标只表示与 DeepSeek Harness 的兼容关系，不构成官方产品或官方背书。当前运行代码仍使用 Lucide `bot` 作为插件图标；本批仅更新 Ardot 审阅真相，运行实现同步明确延期到新的获批批次。
- 保持原生字体栈，不引入品牌字体或独立网页皮肤。
- 垂直方向紧凑、横向留白充足；默认主内容内边距为上 `24px`、水平 `28px`、下 `32px`。
- 结构以平面边框和背景分区为主，使用 `--radius-s`，不使用大面积渐变、浮夸阴影、玻璃拟态或装饰插画。
- 每页只有一个主内容区和一个最高优先级操作；次要信息降低视觉权重。
- 状态、空态、限制和失败必须使用可读文字，不得留下空白歧义。
- 产品界面不得展示开发阶段、发布门或治理审批；这些信息只存在于仓库治理材料。暂不可用导航统一使用 `--text-faint` 与低权重图标，并保留可访问的禁用语义。

## 6. 交互与无障碍

- 所有可用导航和操作使用原生 `button`、`nav`、`aside`、`header` 等语义元素。
- 当前页面使用 `aria-current="page"`；检查或任务执行中使用 `aria-busy="true"`。
- 暂不可用导航使用原生 `disabled` 或 `aria-disabled="true"`，文字与图标采用浅灰禁用态，不显示额外状态徽标。
- “新建任务”实现后，空输入和缺少必需权限时发送操作必须不可执行；键盘焦点顺序依次覆盖模式、输入、上下文、权限和发送。
- 键盘焦点必须可见，颜色不是表达状态的唯一手段。
- 点击 ribbon 或命令重复打开 Workbench 时复用已有标签页，不生成重复工作区。
- 打开快速助手只影响显式的右侧命令，不改变中央 Workbench 的页面状态。

## 7. 真值矩阵

| 表面 | 当前状态 | 允许显示 | 禁止暗示 |
| --- | --- | --- | --- |
| 中央 Workbench 壳层 | 已实现并通过既有运行验收 | 当前概览、内部导航、运行状态 | v2 导航与新建任务已经同步 |
| 新建任务 | v2 已设计，运行未实现 | 目标输入结构与执行边界；未同步前不渲染为可用页面 | 开发阶段或发布门文案；已可对话、执行任务或读取上下文 |
| 运行 | v2 已设计，当前实现仍为概览 + 运行状态 | 概览真值、只读健康检查和明确状态 | 合并页面已经运行验收 |
| DSH 健康检查 | 已实现并验证 | 固定 `--version` 结果 | 已建立会话或协议连接 |
| 快速助手 | 当前空容器已实现；v2 定义为可选 | 健康、上下文空态、能力边界 | 主对话入口或必需功能 |
| 会话 | 未实现 | 不暴露，或以真实禁用操作表达 | 开发状态文案、模型、回复、历史 |
| Vault | 未启用 | “未启用” | 已读取、索引或可写 |
| 未来模块 | 仅规划 | 浅灰文字与图标的禁用入口 | “规划中”等进度徽标；任务、阅读、资讯或领域数据 |

## 8. 变更同步

修改 Workbench 布局、页面、导航、图标、状态文案或响应式规则时，必须先在 Ardot 中形成可审阅变化并获得用户批准，再在同一实现批次同步检查：

- Ardot 项目中的最新用户审阅版本、页面/画板 ID 和设计系统说明；
- `src/main.ts`、相关 ItemView 和状态模型；
- `styles.css`；
- 插件基线测试、治理契约测试与 Obsidian mock；
- `README.md`、架构 ADR 和 `design-qa.md`；
- `scripts/verify-ci-coverage.mjs` 与 GitHub Actions 是否实际执行对应测试；
- 宽屏、窄屏、浅色、深色、键盘与错误状态的隔离 Vault 验收需求。

若代码、文档或运行结果暂时未同步 Ardot，必须把差异标记为`仅设计已更新，运行代码未同步`或`实现领先，Ardot 未同步`，不得把两者都称为当前真相。UI 批次的 CI 必须执行治理契约测试，校验 Ardot 链接、审阅版本和同步规则仍然存在；视觉像素本身由 `design-qa.md` 的同视口截图比较验收。

未经新的明确批准，不把规划入口升级为可用能力，也不增加公共模块注册框架。
