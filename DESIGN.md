# DeepSeek Harness Workbench 设计规范

状态：已批准，作为本仓库 UI 设计权威。

本文件只记录稳定的产品布局、视觉规则与真实状态表达。逐批视觉验收证据记录在 `design-qa.md`；实时能力以源码、测试和运行读回为准。

## 1. 产品定位

DeepSeek Harness Workbench 是 Obsidian 内的中央智能体工作台，不是常驻聊天侧边栏，也不复制 Obsidian 自己的标题栏、ribbon、标签栏、文件导航或全局设置。

当前唯一主工作流是：用户从 Obsidian 最左侧 ribbon 或命令面板打开一个可复用的中央 Workbench 标签页，在插件自有导航中查看概览和运行状态；需要时可以单独打开右侧快速助手容器。

## 2. 参考层级

设计判断依次参考：

1. 本文件和已接受的架构 ADR。
2. 当前仓库的真实源码、测试、构建与 Obsidian 运行结果。
3. Trend Radar 仓库 `DESIGN.md` 中已经验证的 Obsidian 原生工作台规则。
4. 用户提供的 WorkBuddy 截图，仅参考“产品自有左导航 + 主内容区”的信息架构，不复制品牌、角色、任务数据、聊天输入、模型选择或其他未实现能力。
5. Ardot 的功能型 Web App 指南，用于校准主区域层级、渐进披露、系统状态可见性和响应式结构。

参考图与本插件能力冲突时，能力真相优先；不得为了视觉完整度制造可点击但不可用的功能或虚构数据。

## 3. 三层工作台结构

### 3.1 Obsidian 宿主入口

- 一个稳定 ribbon 图标打开或复用中央 Workbench 标签页。
- 命令面板提供相同入口。
- Workbench 不默认占用 Obsidian 右侧边栏。
- 插件不重建 Obsidian 全局 chrome。

### 3.2 Workbench 内部导航

- 中央标签页内由插件拥有一条左侧本地导航栏。
- 桌面宽屏导航宽度固定为 `194px`。
- 当前可用页面只有“概览”和“运行状态”。
- “助手”“项目”“专家 · Skill · 连接器”“自动化”“资料库”“领域工作台”只显示为禁用的“规划中”入口。
- 规划入口不得注册点击行为、加载假数据或暗示对应能力已经可用。
- 小于或等于 `760px` 的容器宽度隐藏固定左栏，改用主内容顶部的紧凑页面选择器。

### 3.3 可选右侧快速助手

- 快速助手是独立、按需打开的 Obsidian 右侧视图，不随 Workbench 自动打开。
- 当前只展示 DSH 健康状态、当前上下文空状态和快捷提问不可用说明。
- 当前不得显示可编辑对话框、发送、停止、模型选择、会话历史或伪造回复。
- 后续会话能力、上下文读取和快捷提问必须分别获得新批次批准并建立安全与 CI 证据。

## 4. 页面职责

### 4.1 概览

概览回答一个问题：当前工作台真实可用到什么程度？

- 主标题、简短说明和“当前真实能力”标识建立页面身份。
- 首屏状态摘要展示工作台壳层、DSH 健康检查、会话能力和 Vault 权限。
- 唯一主操作是进入“运行状态”完成只读 DSH 检查。
- 页面必须明确说明当前不启动会话、不读取或写入 Vault、不发起模型请求。

### 4.2 运行状态

运行状态回答一个问题：当前配置的 DSH 命令能否通过受限的 `--version` 检查？

- 保留连接状态、运行时健康检查、Vault 权限和平台四项真实状态。
- 唯一主操作是“检查 DSH”。检查中禁用重复操作并显示进行中状态。
- 健康检查成功只表示命令可执行且版本匹配，不表示 DSH 已连接。

## 5. 视觉语言

- 使用 Obsidian 语义变量：`--background-primary`、`--background-secondary`、`--background-modifier-border`、`--background-modifier-hover`、`--interactive-accent`、`--text-normal`、`--text-muted` 和 `--text-faint`。
- 使用 Obsidian 内置 Lucide 图标；图标辅助识别但不替代文字标签。
- 保持原生字体栈，不引入品牌字体或独立网页皮肤。
- 垂直方向紧凑、横向留白充足；默认主内容内边距为上 `24px`、水平 `28px`、下 `32px`。
- 结构以平面边框和背景分区为主，使用 `--radius-s`，不使用大面积渐变、浮夸阴影、玻璃拟态或装饰插画。
- 每页只有一个主内容区和一个最高优先级操作；次要信息降低视觉权重。
- 状态、空态、限制和失败必须使用可读文字，不得留下空白歧义。

## 6. 交互与无障碍

- 所有可用导航和操作使用原生 `button`、`nav`、`aside`、`header` 等语义元素。
- 当前页面使用 `aria-current="page"`；检查中使用 `aria-busy="true"`。
- 禁用的规划入口使用原生 `disabled`，并显示“规划中”。
- 键盘焦点必须可见，颜色不是表达状态的唯一手段。
- 点击 ribbon 或命令重复打开 Workbench 时复用已有标签页，不生成重复工作区。
- 打开快速助手只影响显式的右侧命令，不改变中央 Workbench 的页面状态。

## 7. 真值矩阵

| 表面 | 当前状态 | 允许显示 | 禁止暗示 |
| --- | --- | --- | --- |
| 中央 Workbench 壳层 | 已实现并需运行验收 | 概览、内部导航、运行状态 | 完整智能体工作台已完成 |
| DSH 健康检查 | 已实现并验证 | 固定 `--version` 结果 | 已建立会话或协议连接 |
| 快速助手 | 容器已实现并需运行验收 | 健康、上下文空态、能力边界 | 可对话、可发送、可取消 |
| 会话 | 未实现 | “尚未实现” | 模型、回复、历史 |
| Vault | 未启用 | “未启用” | 已读取、索引或可写 |
| 未来模块 | 仅规划 | 禁用入口和“规划中” | 任务、阅读、资讯或领域数据 |

## 8. 变更同步

修改 Workbench 布局、页面、导航、图标、状态文案或响应式规则时，必须同步检查：

- `src/main.ts`、相关 ItemView 和状态模型；
- `styles.css`；
- 插件基线测试、治理契约测试与 Obsidian mock；
- `README.md`、架构 ADR 和 `design-qa.md`；
- `scripts/verify-ci-coverage.mjs` 与 GitHub Actions 是否实际执行对应测试；
- 宽屏、窄屏、浅色、深色、键盘与错误状态的隔离 Vault 验收需求。

未经新的明确批准，不把规划入口升级为可用能力，也不增加公共模块注册框架。
