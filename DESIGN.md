# DeepSeek Harness Workbench 设计规范

状态：已批准的产品 UI 目标；宿主 UI、只读知识库、真实对话、Vault 外任务、文件审核/撤销、同 leaf 正式会话与原生任务环境均已实现。Batch 10 已通过专用 Vault 的宽窄屏、明暗主题、键盘、错误恢复、真实文件变更/撤销和同步卸载零残留技术门，以及实现 SHA 的双平台 CI 与原始零 annotations；用户最终 Obsidian UI 明确验收仍待完成。最新获用户批准的 [Ardot 设计项目](https://ardot.tencent.com/file/718186366720195)是用户审阅的产品 UI 界面真相；本文件是其稳定文字契约与实现镜像。

Ardot 负责页面、布局、状态、图标、文案层级和响应式外观的审阅真相；本文件负责把已批准设计转写成可测试规则。源码、测试、CI 和运行读回仍是“是否已经实现并可用”的能力真相，Ardot 画板不得被解释为功能已经交付。逐批视觉验收证据记录在 `design-qa.md`。

## 0. Ardot 审阅基线

- 项目：`DeepSeek Harness Workbench · UI 真相`
- 文件 ID：`718186366720195`
- 用户审阅版本：`v2`，批准日期 `2026-08-24`
- 页面：`UI 真相 v2`（`12:1`）；`UI 真相 v1`（`0:1`）只作为历史与失败回退基线保留。
- 产品画板：`00 v2 设计系统与交互状态`（`12:2`）、`01 新建任务 宽屏浅色`（`12:41`）、`02 运行 宽屏浅色`（`12:120`）、`03 新建任务与快速助手 宽屏浅色`（`12:191`）、`04 新建任务 宽屏深色`（`12:275`）、`05 新建任务 700px 容器`（`12:360`）。
- 同屏 QA 画板：`06 参考截图与 v2 对照 QA`（`12:530`）。

只有最新获用户批准的 Ardot 版本可以授权实现同步；未批准草稿、实验副本和导出截图都不能单独改变产品能力或开发范围。Ardot 是用户审阅和完善 UI 的界面，AI 默认只读；除非用户对当前批次明确要求修改 Ardot，否则只能读取和对照，不得写入。

`2026-08-26` 用户直接反馈仅授权插件实现差异：未实现模块不在插件导航中渲染，任务模式分段控件在插件中使用左右半圆胶囊边界。Ardot v2 未修改，仍保留其用户审阅状态；该差异由本文件、ADR、测试和 `design-qa.md` 追踪。

`2026-08-28` 用户继续授权插件路线演进：整体 UI 后续评估 Codex 的会话导航、中央工作流与可选右侧栏；首条消息确认发送后，开启页在同一个 Workbench leaf 内切换为正式会话页；同时按 Obsidian 原生插件边界保留 DSH 能力。该反馈只进入插件、文字契约、测试和 CI，Ardot 未修改、只读核对。详细取舍与验收目标见 [`docs/design/codex-reference-ui-assessment.md`](./docs/design/codex-reference-ui-assessment.md)。

`2026-08-31` 用户确认后续产品把“上下文”“工作环境”和“权限级别”拆为三层：当前 Obsidian Vault 自动成为可读写知识库，笔记/文件夹只表达本次上下文，外部工作区单独选择，权限选择器只保留“标准权限”和“完全权限”。两档都默认开放 Shell 与网络，共享相同的已安装、已启用 Skill、MCP Tools 及其他完整能力；权限只改变本地文件作用范围。标准权限可读写当前知识库和已选外部工作区，越界本地文件访问在运行中授权；完全权限允许当前 Windows 用户可访问范围内的全磁盘访问。该决策记录在 [ADR-011](./docs/architecture/ADR-011-context-environment-and-permission-levels.md)，目前仅为后续产品契约，尚未实现；Vault 高风险变更、stdio MCP 宿主边界和权限默认值仍待讨论。Ardot 未修改、只读核对，当前 v1 能力真相不变。

## 1. 产品定位

DeepSeek Harness Workbench 是 Obsidian 内的中央智能体工作台，不是常驻聊天侧边栏，也不复制 Obsidian 自己的标题栏、ribbon、标签栏、文件导航或全局设置。

目标主工作流是：用户从 Obsidian 最左侧 ribbon 或命令面板打开一个可复用的中央 Workbench 标签页，默认进入导航首位的“新建任务”，与 DeepSeek Harness 对话、定义任务、选择知识库内容并在执行前审阅权限与变更；需要时可以单独打开右侧任务环境。导航最后的“运行”汇总概览真值与只读运行状态。

当前源码以 v2 为审阅基线，并应用 `2026-08-26` 至 `2026-08-29` 的插件直接反馈：默认“新建任务”、合并后的“运行”、三行品牌、鲸鱼图标、宽窄响应式均已实现；未开放模块不进入插件导航，模式分段控件采用左右半圆胶囊边界；知识库入口使用“选择知识库 / 已选笔记”，选择项不使用方框阴影并提供文件夹入口。Batch 7 加入真实只读对话、发送前确认、流式回复、停止和明确终态；Batch 8C/8D 接通单一 Vault 外工作区、任务控制器、逐请求权限、真实文件卡、审核、原生菜单和安全撤销。Batch 9 在同一 leaf 中实现开启页到正式会话页切换、显式新建任务与插件生命周期内的恢复边界，并把原快速助手原位演进为默认关闭的“任务环境”；附件与代码协作继续真实禁用。`2026-08-28` 的 Codex/DSH 截图只作为插件路线参考，不授权克隆桌面 chrome、私有推理、完全权限或 DSH 配置中心。上述直接反馈只授权修改插件、文字镜像、测试和证据，Ardot 未修改。

## 2. 参考层级

视觉与布局判断依次参考：

1. 最新获用户批准的 Ardot 审阅版本。
2. 本文件和已接受的架构 ADR。
3. 当前仓库的真实源码、测试、构建与 Obsidian 运行结果，用于判断实现差异和能力真相。
4. Trend Radar 仓库 `DESIGN.md` 中已经验证的 Obsidian 原生工作台规则。
5. 用户提供的 WorkBuddy 截图，仅参考“首位新建任务 + 产品自有左导航 + 中央任务输入”的信息架构，不复制品牌、账号、历史任务、模型名称、第三方服务入口或其他未实现数据。

参考图与本插件能力冲突时，能力真相优先；不得为了视觉完整度制造可点击但不可用的功能或虚构数据。

## 3. 三层工作台结构

### 3.1 Obsidian 宿主入口

- 一个稳定 ribbon 图标打开或复用中央 Workbench 标签页。
- 命令面板提供相同入口。
- Workbench 不默认占用 Obsidian 右侧边栏。
- 插件不重建 Obsidian 全局 chrome。

### 3.2 Workbench 内部导航

- 中央标签页内由插件拥有一条左侧本地导航栏。
- 桌面宽屏导航宽度固定为 `194px`。
- 导航首位固定为“新建任务”；当前已在同一入口接通只读主对话、知识库上下文、外部工作区任务、逐请求权限和变更审阅。
- Ardot v2 审阅基线仍在中间呈现“项目”“专家 · Skill · 连接器”“自动化”“资料库”“领域工作台”的浅灰禁用态；`2026-08-26` 用户直接反馈要求当前插件不渲染这些未实现模块，且未授权修改 Ardot。
- “概览”和“运行状态”合并为“运行”，固定在功能导航最后；当前审阅名为“运行”。只有用户明确批准修改 Ardot 时，后续改名才同步 Ardot；仅授权插件改名时只更新本文件、ADR、实现、测试和 CI 契约并登记差异。
- 右侧“任务环境”不占用内部功能导航项；它是单独命令或正式页按钮按需打开的可选视图。
- 未实现模块不得在插件导航或紧凑选择器中渲染，也不得加载假数据或暗示对应能力已经可用。
- 小于或等于 `760px` 的容器宽度隐藏固定左栏，改用主内容顶部的紧凑页面选择器。
- 当前源码只按“新建任务”“运行”顺序渲染，不再提供分离的“概览”“运行状态”、内部“助手”或五个未开放模块入口。

### 3.3 可选右侧任务环境

- 原“快速助手”的既有 view type 与命令 ID 原位演进为一个按需打开的 Obsidian 原生 `ItemView`，产品标题与命令文案为“任务环境”；不随 Workbench 自动打开，重复开启复用同一右侧 leaf。
- 当前只投影 DSH 健康/连接、已选笔记、外部工作区名称、当前权限、模型/预设事实边界、已观察工具和最近一轮账本变更；不复制主消息流，也不承载会阻断任务的唯一确认。
- 模型/预设具体标识在当前协议未公开，因此只显示“由 DSH 配置管理”；不得显示私有推理、完整绝对路径、假历史或假指标。
- 中央正式会话在右侧栏关闭时完整可用；`700px` 容器默认关闭右侧栏并折叠内部导航。
- Codex 三栏界面只提供信息关系参考；插件不复制 Codex 桌面窗口、Git 操作栏或项目管理器。

## 4. 页面职责

### 4.1 新建任务

“新建任务”是 v2 的核心工作流，也是唯一主对话与任务入口。

- 删除 Workbench 页面上方由 Obsidian 宿主居中显示的 `DeepSeek Harness Workbench` 可见标题文字；只删除这一处文字，保留标签页名称、鲸鱼图标、标签栏、返回/前进、更多菜单和其他 Obsidian 宿主控件。
- 新建任务页不重复显示顶部页标题/说明；中央主标题、模式、输入区和执行确认只显示产品语义，不显示“首发目标”“尚未实现”、开发阶段、发布批次或治理审批文案。
- 首屏提供“对话”“任务执行”“代码协作”模式层级；分段控件在插件中使用左右半圆胶囊边界。“对话”真实可发送；任务执行已接通单一 Vault 外工作区，只有原生目录选择与统一边界校验成功后才允许发送；“代码协作”保持禁用。
- 主输入区包含目标描述、`@` 上下文引用、`/` Skill 或命令入口、附件、“选择知识库”、默认权限、DSH 模型管理提示和发送操作；选择结果区标题固定为“已选笔记”。
- 输入、知识库内容、权限、模型和发送都不得使用假数据；当前选择只显示用户明确加入的真实 Vault 相对路径或选区预览。当前选区指用户先在 Markdown 编辑器中选中的文本，加入时冻结；不可用时必须提示先在编辑器中选择。对话发送前以 modal 列出任务和已选笔记，明确“不开放 DSH 工具、不写入知识库”；确认后才重读文件、建立快照并启动 DSH。空草稿、非对话模式或活动 turn 均不得产生第二次发送。
- “选择文件夹”递归枚举所选文件夹当下已有的 Markdown 笔记并原子加入，排除已经选择的笔记；根 Vault 不作为文件夹候选。选择结果冻结为逐篇笔记 ID，不持续跟踪后来新增的文件；数量或字节超限时整批失败，不允许部分加入。
- 对话消息区使用一层无阴影边框和介于 `--background-secondary` 与 `--background-primary` 之间的浅色背景，消息之间保持平面分隔；输入框采用四行紧凑高度。流式回复只更新消息区，不重绘输入框。活动 turn 的主按钮变为“停止”；取消请求只有收到 bridge 取消终态才能显示“已停止”，超时终止显示失败。任务模式在 composer 上方显示工作区名称和“Vault 外目录 · 仅本次任务会话可写 · 文件工具逐次确认”，发送前再次审阅边界；终态显示账本确认的文件数和文本增删摘要，无法核对时显示失败而不是完成。完整绝对路径不进入普通 UI。Vault 写入仍遵循独立审批协议。
- 任务 turn 文件卡沿用同一无阴影浅底区域与一层分隔线；默认列出三个相对路径，超出项通过带 `aria-expanded` 的按钮展开。主点击打开真实修改前/后快照，右键使用 Obsidian `Menu` 提供默认应用、资源管理器和显式复制操作；每次操作前重新校验工作区、路径与普通文件。已删除文件不伪装存在。审核预览只截断 UI，不改变账本事实。
- “撤销”始终先打开二次确认，列出整个 turn 的全部文件；只有账本完整性、工作区身份和全部当前 SHA 仍匹配时才一次执行。任一冲突显示零写入错误并保持窗口打开，成功后卡片显示“已撤销”且不能重复撤销。v1 不提供 VS Code 专属入口、桌面“打开方式”克隆或“另存为”，避免未验证外部依赖和账本之外的写入面。

### 4.2 正式会话页（已实现并通过 Batch 10 专用 Vault 技术运行门）

- “今天想让 DeepSeek Harness 做什么？”只属于尚未开始 session 的 `startup` 状态；输入文本不会提前切页，只有首条消息经发送前确认后才进入 `starting` 并切换到正式会话。
- 开启页和正式会话使用同一个 Workbench leaf、同一个控制器与同一个 session 投影，不创建浏览器路由、第二个 Workbench 标签或重复 composer。
- 正式页顶部显示会话标题、真实运行状态和工作区/权限简要摘要；中央显示公开消息、工具名称、权限请求、已编辑文件和明确终态；底部保留紧凑、可增长的 composer。
- 开启页主标题与正式页会话标题互斥。活动 turn 的主操作为停止，空闲时继续同一 session；“新建任务”必须是显式操作，重复点击 ribbon 不重置当前会话。
- 不显示 DSH 私有推理、未经官方公开契约读回的 token/速度/缓存指标、完整本机绝对路径、虚构历史或未实际启用的能力。
- 最近会话只有在 DSH 官方 session 恢复契约和真实读回通过后才进入导航；此前只显示当前插件生命周期内的真实会话。

### 4.3 运行

“运行”合并原“概览”和“运行状态”，回答两个问题：当前工作台真实可用到什么程度，以及当前配置的 DSH 命令能否通过受限的 `--version` 检查？

- 首屏概览摘要展示工作台壳层、DSH 健康检查、会话能力和 Vault 权限。
- 下方保留连接状态、运行时健康检查、Vault 权限和平台四项真实状态。
- 唯一主操作是“检查 DSH”。检查中禁用重复操作并显示进行中状态。
- 健康检查成功只表示命令可执行且版本匹配，不表示 DSH 已连接。

### 4.4 首个社区发布门

首个 Obsidian 社区插件发布功能固定为“新建任务”。只有同时满足以下条件，才允许进入社区发布审批：

1. “新建任务”的对话、任务执行、上下文、权限与错误状态完整实现。
2. 测试和双平台 CI 实际覆盖相应行为，隔离 Vault 运行验收通过。
3. 用户对最终 Obsidian 运行界面完成 UI 验收并明确批准。

Ardot 设计通过、CI 成功或 GitHub Release 单独成立时，都不能越过此发布门。

## 5. 视觉语言

- 使用 Obsidian 语义变量：`--background-primary`、`--background-secondary`、`--background-modifier-border`、`--background-modifier-hover`、`--interactive-accent`、`--text-normal`、`--text-muted` 和 `--text-faint`。
- 插件身份图标使用 DeepSeek Harness Web 前端 `@deepseek-ai/dsh-web-frontend@0.1.1-rc.1/dist/favicon.svg` 的鲸鱼矢量，源文件 SHA-256 为 `C61A62A9D47D8660F9CFE08AAC6775FF0476F7D6C5053F7659C1F8493FD6D814`；rc.2 运行夹具中的同名资产哈希一致。Ardot v2 中用于 Obsidian ribbon、活动标签页、Workbench 左上角和原“快速助手”，插件 Batch 9 对应位置已改为“任务环境”；深色模式以白色承载底保证黑色原始矢量的对比度，不重绘品牌图形。
- Workbench 左上角品牌名称固定为三行：第一行 `DeepSeek`、第二行 `Harness`、第三行 `Workbench`；不得合并成两行或增加副标题。
- 其他功能图标使用 Obsidian 内置或官方 Lucide 图标；图标辅助识别但不替代文字标签。“新建任务”使用 Lucide `circle-plus.svg`，来源 SHA-256 为 `6FA978530075DF975AD05E2742896A1950B74C2B133A0D208891B038A4A41C17`。
- 鲸鱼图标只表示与 DeepSeek Harness 的兼容关系，不构成官方产品或官方背书。当前实现已注册同一鲸鱼 path 几何，并用于 ribbon、活动标签页、Workbench 左上角和任务环境；图标在 Obsidian 宿主表面使用 `currentColor`，品牌承载底保持黑色原图与白色底的对比关系。
- 保持原生字体栈，不引入品牌字体或独立网页皮肤。
- 垂直方向紧凑、横向留白充足；默认主内容内边距为上 `24px`、水平 `28px`、下 `32px`。
- 结构以平面边框和背景分区为主，使用 `--radius-s`，不使用大面积渐变、浮夸阴影、玻璃拟态或装饰插画。
- 知识库来源选择器中的选项保留清楚边框与悬停状态，但方框阴影固定为 `none`；每项使用标题与次级说明解释读取时机和范围。
- 每页只有一个主内容区和一个最高优先级操作；次要信息降低视觉权重。
- 状态、空态、限制和失败必须使用可读文字，不得留下空白歧义。
- 产品界面不得展示开发阶段、发布门或治理审批；这些信息只存在于仓库治理材料。当前未实现模块不在插件导航中渲染；仍需保留的不可用操作使用 `--text-faint` 与原生禁用语义。

## 6. 交互与无障碍

- 所有可用导航和操作使用原生 `button`、`nav`、`aside`、`header` 等语义元素。
- 当前页面使用 `aria-current="page"`；检查或任务执行中使用 `aria-busy="true"`。
- 未实现模块不进入插件导航；“代码协作”、附件和任务模式发送等仍需展示的不可用操作使用原生 `disabled` 或 `aria-disabled="true"`，不显示额外状态徽标。“选择知识库”和对话模式发送是当前可用操作；权限按钮只在 bridge 发出当前 session/turn/request 的请求时出现，并且只提供“拒绝 / 仅本次允许”。
- “新建任务”实现后，空输入和缺少必需权限时发送操作必须不可执行；键盘焦点顺序依次覆盖模式、输入、知识库选择、权限和发送。
- 键盘焦点必须可见，颜色不是表达状态的唯一手段。
- 点击 ribbon 或命令重复打开 Workbench 时复用已有标签页，不生成重复工作区。
- 打开任务环境只影响显式的右侧命令，不改变中央 Workbench 的页面状态。
- 正式会话切换后，消息与运行状态使用 `aria-live`，活动执行容器使用 `aria-busy="true"`；状态不只依赖颜色。
- 键盘必须覆盖开启页、发送前审阅、正式会话、停止、文件展开、审核、撤销确认与右侧环境栏开关；焦点返回触发操作或安全的会话位置。
- 精确 `700px` 容器不得出现水平滚动；内部导航折叠、右侧栏关闭时仍可完成主任务。

## 6.1 DSH 原生能力投影

- DSH 原生配置继续是模型、提供方、插件、Agent 预设、凭据与完整 session 的唯一事实源；插件设置页只显示当前公开摘要并提供打开原生配置位置，不复制配置数据库或凭据。
- 插件只投影当前 session 真实采用且协议可证明的模型/预设标识，以及实际启用的工具名称与公开事件；未启用或未经兼容验证的能力不显示。
- 当前 v1 产品权限只保留“只读对话”和“仅本次可写外部工作区”；当前实现不提供完全权限、跨会话永久授权、任意 Shell、网络或隐式整库访问。后续已接受但尚未实现的标准/完全权限与能力一致性契约见 ADR-011。
- DSH 完整历史由原生 session 保存；插件只有在官方恢复契约通过后才可在 Vault 外维护最小索引。私有推理和未经证实的性能指标不进入 UI 或插件状态。

## 7. 真值矩阵

| 表面 | 当前状态 | 允许显示 | 禁止暗示 |
| --- | --- | --- | --- |
| 中央 Workbench 壳层 | 用户反馈差异已实现并通过专用隔离 Vault 验收 | 只显示新建任务与运行 | 真实 DSH 已连接 |
| 新建任务 | Batch 6–10 的实现、专用 Vault 技术运行门、双平台 CI 与原始零 annotations 已通过；用户最终 UI 验收待闭环 | 模式、内存草稿、显式只读知识库、单一 Vault 外工作区、逐次权限、真实流式回复、停止、文件卡、审核、撤销、正式会话与错误终态 | 跨重启恢复、最近会话或用户最终 UI 验收已通过 |
| 运行 | 合并页面已实现；会话连接状态由插件级控制器真实投影 | 概览真值、只读健康检查、已连接/未连接和权限边界 | 健康检查成功等于会话已连接 |
| DSH 健康检查 | `0.1.1-rc.2` 目标、正式 bridge、远端 CI 与专用隔离 Vault 读回已通过 | 固定 `--version` 结果 | 已建立会话或协议连接 |
| 任务环境 | Batch 9 已通过本地门与双平台 CI；Batch 10 已通过专用 Vault 的右侧 leaf、真实投影与窄屏技术运行门 | 健康/连接、已选笔记、工作区名称、权限、DSH 配置边界、已观察工具与最近变更 | 主对话、完整路径、私有推理、具体模型/预设或必需确认 |
| 会话 | Batch 7 插件级只读对话与 Batch 10 完整技术运行门已通过；最终用户 UI 明确验收待完成 | 当前插件生命周期内的用户/助手文本、流式状态、停止和明确终态；完整历史由 DSH 原生 session 保存 | 已恢复跨重启历史、已开放未授权工具或 Vault 写入 |
| 正式会话页与右侧环境栏 | Batch 9–10 已通过实现、本地门、专用 Vault 技术运行门、双平台 CI 与原始零 annotations；用户最终 UI 验收待完成 | 同 leaf 切换、显式新建任务、插件生命周期内恢复与公开环境投影 | 跨重启恢复、最近会话、第二套 DSH 配置或最终用户验收 |
| DSH 配置与能力 | DSH 原生配置为事实源；当前插件只管理 DSH 命令和运行投影 | 当前实际模型/预设/工具的公开摘要，及打开原生配置位置 | 插件已复制提供方、凭据、插件、预设或完整 session 数据库 |
| Vault | 只读知识库选择已实现并通过反馈后的专用 Vault 运行验收 | 仅显式选中的 Markdown 文件/选区、文件夹当下冻结的确定笔记集合、Vault 相对路径、移除与限制错误 | 隐式整库内容读取、持续索引、写入、删除或移动 |
| 未来模块 | 仅规划 | 不在插件导航中渲染 | 入口、进度徽标、任务、阅读、资讯或领域数据 |

## 8. 变更同步

修改 Workbench 布局、页面、导航、图标、状态文案或响应式规则时，先读取最新获用户批准的 Ardot 基线。只有用户明确要求修改 Ardot 时才允许写入；未获授权时只修改插件并登记差异。在同一实现批次同步检查：

- Ardot 项目中的最新用户审阅版本、页面/画板 ID 和设计系统说明；
- `src/main.ts`、相关 ItemView 和状态模型；
- `styles.css`；
- 插件基线测试、治理契约测试与 Obsidian mock；
- `README.md`、架构 ADR 和 `design-qa.md`；
- `scripts/verify-ci-coverage.mjs` 与 GitHub Actions 是否实际执行对应测试；
- 宽屏、窄屏、浅色、深色、键盘与错误状态的隔离 Vault 验收需求。

若代码、文档或运行结果与 Ardot 不同，必须记录用户直接反馈、Ardot 未修改和插件实现差异，不得把两者都称为已经同步。UI 批次的 CI 必须执行治理契约测试，校验 Ardot 链接、审阅版本、AI 只读边界和差异规则仍然存在；插件视觉由 `design-qa.md` 的同视口截图验收。

未经新的明确批准，不把规划入口升级为可用能力，也不增加公共模块注册框架。
